---
description: Claude Sounds Plugin - manage hooks and themes (/cs help)
argument-hint: [help|status|enable|disable|hook|theme|project|test|ui]
allowed-tools: Bash
---

Run the following command using the Bash tool and print the output exactly as-is, without any additional commentary, interpretation, or rephrasing:

```bash
bash "${CLAUDE_PLUGIN_ROOT}/scripts/manage.sh" $ARGUMENTS
```
